<Window x:Class="WPF.MVVM.Cars.View.CarDetailView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.MVVM.Cars.View"
        mc:Ignorable="d"
        Title="CarDetailView" Height="400" Width="600">
    <Grid>
        <StackPanel Width="auto" HorizontalAlignment="Center">
            <WrapPanel>
                <Label Content="Car Id : "></Label>
                <Label Content="{Binding Id}"></Label>
            </WrapPanel>
            <WrapPanel>
                <Label Content="Name : "></Label>
                <TextBox Text="{Binding Name, Mode=OneWay}" Width="300"></TextBox>
            </WrapPanel>
            <WrapPanel>
                <Label Content="Cost : "></Label>
                <TextBox Text="{Binding Price, Mode=OneWay}" Width="300"></TextBox>
            </WrapPanel>
            <WrapPanel>
                <Label Content="Country of Origin : "></Label>
                <TextBox Text="{Binding Country, Mode=OneWay}" Width="300"></TextBox>
            </WrapPanel>
            <WrapPanel>
                <Label Content="Type : "></Label>
                <TextBox Text="{Binding Type, Mode=OneWay}" Width="300"></TextBox>
            </WrapPanel>
        </StackPanel>
    </Grid>
</Window>
